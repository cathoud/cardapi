server:
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: on_param
    include-exception: false

spring:
  datasource:
    url: jdbc:mysql://localhost/db_cards
    username: root
    password: example
